// src/input.cpp
/**
 * @file input.cpp
 * @brief Translation unit for input mapping. The implementation is inline in input.hpp.
 *
 * Keeping this TU ensures builds that glob src/*.cpp succeed even if input handling
 * remains header-only for inlining convenience.
 */
#include "input.hpp"
